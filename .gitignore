# Conversation history (contains sensitive data)
history.jsonl

# Project-specific settings (contains local paths and project data)
projects/

# Session and runtime data
sessions/
*cache/
debug/
file-history/
plans/
session-env/
shell-snapshots/
todos/
ide/
backups/
mcp-needs-auth-cache.json

# Analytics and telemetry
stats-cache.json
statsig/
telemetry/
security_warnings_*

# Plugins (local state and caches)
plugins/

# Tasks
tasks/

# OS-specific
.DS_Store

# Ignore claude's settings inside .claude :)
.claude/
